export default function Searchbar(){
    return(
        <form id="searchBar">
        <label htmlFor="searchTerm">
          Search by Article Id
        </label>
        <input/>
        <button>Submit</button>
      </form>
    )
}